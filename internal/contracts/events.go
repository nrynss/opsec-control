package contracts

// events.go — the Event struct, the event-type enum, and per-type payload
// shapes (SPEC §7).
//
// Placeholder: no shapes are defined yet. The Event taxonomy in SPEC §7 is
// populated here via the §0.5 coordinated step, NOT unilaterally by a consuming
// package. Mirror any JSON shape under internal/contracts/schemas/.
