package contracts

// agentio.go — the Cell input envelope (state snapshot + triggering event) and
// the per-Cell structured output schemas (SPEC §9).
//
// Placeholder: no shapes are defined yet. Each Cell is a pure function of
// (state snapshot + triggering event) -> structured output; this file is the
// only schema Cells and the orchestrator share. Populate via the §0.5 step.
