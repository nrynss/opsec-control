package contracts

// state.go — World State types, entity types, and their status enums
// (SPEC §8.2–§8.4: bridges, dam, levee, power, comms/water/gas, hospitals,
// shelters, fire zones, flood, resources, …).
//
// Placeholder: no shapes are defined yet. Entity types and the legal-transition
// status enums (§8.4) are added here only via the §0.5 coordinated step. The
// sole mutator of live state remains internal/state — these are just the types.
