package contracts

// errors.go — shared error and rejection types. Chiefly the event_rejected
// reasons emitted by the §14.2 validation contract (schema, referential
// integrity, temporal monotonicity, illegal transition, range/sanity,
// idempotency) so rejections are logged and debuggable.
//
// Placeholder: no shapes are defined yet. Populate via the §0.5 step.
