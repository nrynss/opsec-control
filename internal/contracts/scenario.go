package contracts

// scenario.go — the scenario file format the offline compiler emits and the
// simulation engine replays (SPEC §14, §11): an ordered, versioned, validated
// stream of events plus the static substrate (sectors, bridges, dam, levee,
// hospitals, shelters, resources — §8.3).
//
// Placeholder: no shapes are defined yet. Populate via the §0.5 step and mirror
// the JSON form under internal/contracts/schemas/ for frontend + validation.
