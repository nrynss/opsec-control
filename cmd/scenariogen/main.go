// Command scenariogen is the offline scenario compiler (Gemma -> validated
// scenario JSON). It runs ahead of the demo and NEVER on the live Cerebras
// request path (SPEC §14.1).
package main

func main() {
	// TODO: drive internal/scenariogen to compile + validate a scenario file.
	// See SPEC §14.
}
